server {
     listen 80;
     server_name localhost;
     location /stub_status {
         stub_status;
     }

     location / {
        root /usr/share/nginx/html;
        index index.html;
     }
}
